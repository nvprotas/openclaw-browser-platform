# Type documentation for DOM observation results.
# In Python these are plain dicts; this module serves as documentation.

# VisibleButtonSummary keys: text, role, type, ariaLabel, selector, href, formAction, dataAttributes, paymentHints
# FormSummary keys: id, name, method, action, inputCount, submitLabels
# ObserveSummary keys: visibleTexts, visibleButtons, forms, urlHints, pageSignatureGuess
